python record.py \
    --robot.type=bi_piper \
    --robot.id=bi-piper-bot \
    --display_data=true \
    --dataset.video=true \
    --dataset.num_episodes=5 \
    --dataset.single_task="pick up all cubes and place it into the box using dual arms" \
    --dataset.repo_id=gmingyng/bipiper

python -m lerobot.record \
        --robot.type=piper \
        --robot.port=can0 \
        --robot.id=arm \
        --display_data=true \
        --dataset.repo_id=gmingyng/piper-dataset-v21 \
        --dataset.num_episodes=5 \
        --dataset.single_task="just relax" \
        --dataset.episode_time_s=5 \
        --dataset.reset_time_s=2

pick & place
python -m lerobot.record \
        --robot.type=piper \
        --robot.port=can0 \
        --robot.id=arm \
        --display_data=true \
        --dataset.repo_id=gmingyng/piper-dataset-v21 \
        --dataset.num_episodes=2 \
        --dataset.single_task="pick up the bottle and place it into the box" \
        --dataset.episode_time_s=30 \
        --dataset.reset_time_s=10

python -m lerobot.record \
        --robot.type=piper \
        --robot.port=can0 \
        --robot.id=arm \
        --display_data=true \
        --dataset.video=true \
        --dataset.num_episodes=5 \
        --dataset.single_task="pick up all cubes and place it into the box" \
        --dataset.episode_time_s=30 \
        --dataset.reset_time_s=8 \
        --dataset.repo_id=gmingyng/piper-pp-allcubev2-1



clean table
python -m lerobot.record \
        --robot.type=piper \
        --robot.port=can0 \
        --robot.id=arm \
        --display_data=true \
        --dataset.video=true \
        --dataset.num_episodes=3 \
        --dataset.single_task="clean the table by putting items into the box" \
        --dataset.episode_time_s=90 \
        --dataset.reset_time_s=15 \
        --dataset.repo_id=gmingyng/piper-clean-table 



python -m lerobot.record \
--robot.type=piper \
--robot.port=can0 \
--robot.id=arm \
--dataset.repo_id=ming/piper-dataset
--dataset.num_episodes=10 \
--dataset.single_task="Pick and place task" \
--dataset.episode_time_s=30 \
--dataset.reset_time_s=10



lerobot-record \
    --robot.type=so100_follower \
    --robot.port=/dev/tty.usbmodem58760431541 \
    --robot.cameras="{laptop: {type: opencv, index_or_path: 0, width: 640, height: 480, fps: 30}}" \
    --robot.id=black \
    --dataset.repo_id=<my_username>/<my_dataset_name> \
    --dataset.num_episodes=2 \
    --dataset.single_task="Grab the cube" \
    --display_data=true

